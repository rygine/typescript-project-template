approvedGitRepositories:
  - "**"

compressionLevel: mixed

enableGlobalCache: false

enableScripts: true

nodeLinker: node-modules

# Not the secure value — restore the 1d default, which skips packages published
# in the last 24 hours, when nothing depends on installing them immediately.
npmMinimalAgeGate: 0
